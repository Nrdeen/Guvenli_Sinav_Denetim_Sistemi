# 🗄️ دليل عرض قاعدة البيانات
# Database Viewer Guide

## 📋 نظرة عامة

هذا الدليل يشرح كيفية عرض البيانات المخزنة في قاعدة بيانات SQLite للنظام، بما في ذلك:
- 👨‍🏫 الأساتذة (Teachers)
- 📝 الاختبارات (Exams)
- 👨‍🎓 الطلاب (Students)
- 📋 التسجيلات (Registrations)
- 🟢 الجلسات النشطة (Active Sessions)
- ⚠️ الانتهاكات (Violations)

---

## 🚀 الطريقة 1: استخدام الأداة البسيطة (موصى بها)

### الخطوة 1: تثبيت المكتبات المطلوبة

```powershell
pip install tabulate
```

### الخطوة 2: تشغيل الأداة

```powershell
cd "Güvenli Sınav Denetim Sistemi"
python simple_db_viewer.py
```

### الخطوة 3: اختيار ما تريد عرضه

ستظهر لك قائمة:
```
1️⃣  - الأساتذة (Teachers)
2️⃣  - الاختبارات (Exams)
3️⃣  - الطلاب (Students)
4️⃣  - التسجيلات (Registrations)
5️⃣  - الجلسات النشطة (Active Sessions)
6️⃣  - ملخص الانتهاكات (Violations Summary)
7️⃣  - عرض الكل (Show All)
8️⃣  - تصدير إلى Excel (Export to Excel)
0️⃣  - خروج (Exit)
```

---

## 📊 الطريقة 2: التصدير إلى Excel

### تثبيت المكتبات الإضافية:

```powershell
pip install pandas openpyxl
```

### التصدير:

1. شغّل الأداة: `python simple_db_viewer.py`
2. اختر الخيار `8` (تصدير إلى Excel)
3. سيتم إنشاء ملف Excel يحتوي على جميع البيانات

---

## 🔧 الطريقة 3: استخدام SQLite Browser (واجهة رسومية)

### تحميل وتثبيت:

1. حمّل **DB Browser for SQLite** من:
   - https://sqlitebrowser.org/dl/

2. ثبّت البرنامج

3. افتح قاعدة البيانات:
   - `Güvenli Sınav Denetim Sistemi/backend/exam_monitoring.db`

### الاستخدام:

- **Browse Data**: لعرض محتويات الجداول
- **Execute SQL**: لتنفيذ استعلامات SQL مخصصة
- **Database Structure**: لعرض بنية قاعدة البيانات

---

## 💻 الطريقة 4: استعلامات SQL مباشرة

### فتح قاعدة البيانات:

```powershell
cd "Güvenli Sınav Denetim Sistemi/backend"
sqlite3 exam_monitoring.db
```

### استعلامات مفيدة:

#### عرض جميع الأساتذة:
```sql
SELECT * FROM teachers;
```

#### عرض جميع الاختبارات:
```sql
SELECT 
    e.exam_name,
    e.exam_code,
    t.full_name as teacher_name,
    e.status
FROM exams e
LEFT JOIN teachers t ON e.teacher_id = t.id;
```

#### عرض جميع الطلاب:
```sql
SELECT * FROM students;
```

#### عرض الطلاب المسجلين في اختبار معين:
```sql
SELECT 
    s.student_id,
    s.full_name,
    er.status,
    er.registered_at
FROM exam_registrations er
JOIN students s ON er.student_id = s.id
JOIN exams e ON er.exam_id = e.id
WHERE e.exam_code = 'PROG2025';
```

#### عرض الانتهاكات لطالب معين:
```sql
SELECT 
    v.violation_type,
    v.severity,
    v.description,
    v.timestamp
FROM violations v
JOIN students s ON v.student_id = s.id
WHERE s.student_id = 'STU001'
ORDER BY v.timestamp DESC;
```

#### إحصائيات الانتهاكات:
```sql
SELECT 
    violation_type,
    COUNT(*) as count,
    AVG(confidence_score) as avg_confidence
FROM violations
GROUP BY violation_type
ORDER BY count DESC;
```

---

## 📝 أمثلة على البيانات

### مثال: عرض اختبار مع طلابه

```sql
SELECT 
    e.exam_name,
    e.exam_code,
    s.student_id,
    s.full_name,
    er.status
FROM exams e
JOIN exam_registrations er ON e.id = er.exam_id
JOIN students s ON er.student_id = s.id
WHERE e.exam_code = 'PROG2025'
ORDER BY s.student_id;
```

**النتيجة المتوقعة:**
```
exam_name              | exam_code | student_id | full_name        | status
-----------------------|-----------|------------|------------------|------------
اختبار البرمجة النهائي | PROG2025  | STU001     | أحمد محمد علي    | registered
اختبار البرمجة النهائي | PROG2025  | STU002     | فاطمة حسن        | registered
اختبار البرمجة النهائي | PROG2025  | STU003     | محمد خالد        | registered
```

---

## 🎯 حالات استخدام شائعة

### 1. التحقق من الطلاب المسجلين في اختبار

```powershell
python simple_db_viewer.py
# اختر: 4 (التسجيلات)
```

### 2. مراقبة الجلسات النشطة

```powershell
python simple_db_viewer.py
# اختر: 5 (الجلسات النشطة)
```

### 3. مراجعة الانتهاكات

```powershell
python simple_db_viewer.py
# اختر: 6 (ملخص الانتهاكات)
```

### 4. تصدير تقرير كامل

```powershell
python simple_db_viewer.py
# اختر: 8 (تصدير إلى Excel)
```

---

## ⚠️ ملاحظات مهمة

1. **قاعدة البيانات يجب أن تكون موجودة**
   - شغّل الخادم مرة واحدة على الأقل لإنشاء قاعدة البيانات

2. **البيانات التجريبية**
   - النظام يأتي ببيانات تجريبية افتراضية
   - يمكنك حذفها أو تعديلها حسب الحاجة

3. **النسخ الاحتياطي**
   - احتفظ بنسخة احتياطية من `exam_monitoring.db` دورياً

4. **الأمان**
   - لا تشارك ملف قاعدة البيانات مع أحد
   - يحتوي على معلومات حساسة للطلاب والأساتذة

---

## 🔍 البحث والتصفية

### مثال: البحث عن طالب معين

```sql
SELECT * FROM students 
WHERE full_name LIKE '%أحمد%' 
   OR student_id LIKE '%001%';
```

### مثال: الاختبارات النشطة فقط

```sql
SELECT * FROM exams 
WHERE status = 'active';
```

### مثال: الانتهاكات الخطيرة

```sql
SELECT 
    s.full_name,
    v.violation_type,
    v.description,
    v.timestamp
FROM violations v
JOIN students s ON v.student_id = s.id
WHERE v.severity IN ('high', 'critical')
ORDER BY v.timestamp DESC;
```

---

## 📞 المساعدة

إذا واجهت أي مشاكل:

1. **قاعدة البيانات غير موجودة**
   ```powershell
   cd "Güvenli Sınav Denetim Sistemi"
   python backend/main.py
   # اضغط Ctrl+C بعد ثوانٍ
   ```

2. **خطأ في الاتصال**
   - تأكد من المسار الصحيح
   - تأكد من صلاحيات القراءة

3. **بيانات فارغة**
   - تحقق من تشغيل الخادم
   - تحقق من وجود البيانات التجريبية

---

## ✅ الخلاصة

الآن يمكنك:
- ✨ عرض جميع بيانات النظام
- 📊 تصدير البيانات إلى Excel
- 🔍 البحث والتصفية
- 📈 إنشاء تقارير مخصصة

**استخدم الأداة المناسبة لاحتياجاتك!** 🎓
# 🎓 دليل نظام الامتحانات الموحد

## نظرة عامة

تم دمج نظامين للامتحانات في منصة واحدة:

### 💻 Sınav Online (الامتحانات عن بُعد)
- **المشروع**: Güvenli Sınav Denetim Sistemi
- **المنفذ**: http://localhost:5000
- **التقنية**: Flask + OpenCV + YOLO
- **الاستخدام**: مراقبة الطلاب أثناء الامتحانات الإلكترونية

### 🏫 Sınav In Class (الامتحانات الصفية)  
- **المشروع**: EduView
- **المنفذ**: http://localhost:3000
- **التقنية**: Reflex + AI Models
- **الاستخدام**: مراقبة الطلاب في الفصل الدراسي

---

## 🚀 طريقة التشغيل

### الطريقة الأسهل (موصى بها):

**انقر مرتين على:** `LAUNCH_SYSTEMS.bat`

سيفتح المتصفح تلقائياً على: `http://localhost:8080`

### من الواجهة:

1. **اختر "Sınav Online"** للامتحانات عن بُعد
   - سيفتح على `http://localhost:5000`
   
2. **اختر "Sınav In Class"** للامتحانات الصفية
   - سيفتح على `http://localhost:3000`

### أو من سطر الأوامر:

```bash
python unified_launcher.py
```

---

## 📋 المتطلبات الأولية

### للنظام الأول (Online):
```bash
# موجود بالفعل - لا يحتاج تثبيت
cd "Güvenli Sınav Denetim Sistemi"
pip install -r requirements.txt
```

### للنظام الثاني (Classroom):
```bash
# يجب تثبيته في المجلد الأعلى
cd ..
git clone https://github.com/Laoode/EduView.git EduView_main
cd EduView_main
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

---

## 🗂️ هيكل المجلدات المطلوب

```
Downloads/
├── Güvenli Sınav Denetim Sistemi/    ← هذا المجلد
│   ├── unified_launcher.py           ← الخادم الموحد
│   ├── launcher_index.html           ← صفحة الاختيار
│   ├── LAUNCH_SYSTEMS.bat            ← ملف التشغيل
│   ├── start_system.py               ← Online system
│   └── ...
│
└── EduView_main/                      ← مجلد EduView (يجب تثبيته)
    ├── object_cheating/
    ├── requirements.txt
    ├── rxconfig.py
    └── ...
```

---

## 🔧 كيف يعمل النظام

### 1. Launcher Server (Port 8080)
- يعرض صفحة اختيار جميلة
- يدير تشغيل النظامين
- يتابع حالة الأنظمة

### 2. عند اختيار "Online":
- يشغل `start_system.py`
- يفتح Flask على port 5000
- يعيد توجيهك للنظام

### 3. عند اختيار "Classroom":
- يبحث عن مجلد `EduView_main`
- يشغل `reflex run`
- يفتح على port 3000
- يعيد توجيهك للنظام

---

## 💡 نصائح الاستخدام

### ✅ للمرة الأولى:

1. شغّل `LAUNCH_SYSTEMS.bat`
2. إذا اخترت "Sınav In Class" وظهر خطأ:
   - افتح terminal جديد
   - نفذ الأوامر التالية:
   ```bash
   cd ..
   git clone https://github.com/Laoode/EduView.git EduView_main
   cd EduView_main
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```
3. ارجع للـ launcher واختر مرة أخرى

### ✅ للاستخدام العادي:

- فقط شغّل `LAUNCH_SYSTEMS.bat`
- اختر النظام المطلوب
- سيفتح تلقائياً

---

## 🎯 اختصارات لوحة المفاتيح

من صفحة الاختيار:
- اضغط `1` → لفتح Online System
- اضغط `2` → لفتح Classroom System

---

## 🔍 حل المشاكل

### المشكلة: Port 8080 مشغول
```bash
netstat -ano | findstr :8080
taskkill /PID <رقم_العملية> /F
```

### المشكلة: EduView لا يعمل
```bash
# تأكد من وجود المجلد:
cd ..\EduView_main

# إذا لم يكن موجوداً:
cd ..
git clone https://github.com/Laoode/EduView.git EduView_main
```

### المشكلة: النظام لا يفتح تلقائياً
- افتح المتصفح يدوياً
- اذهب إلى `http://localhost:8080`

---

## 📊 المقارنة بين النظامين

| الميزة | Online | Classroom |
|--------|--------|-----------|
| المنفذ | 5000 | 3000 |
| التقنية | Flask | Reflex |
| الكشف | Face/Eye/Mouth/Object | 3 AI Models |
| التقارير | JSON + HTML | Interactive |
| الاستخدام | عن بُعد | في الفصل |

---

## 📞 الدعم

- **النظام الأول**: راجع `Proje_Dokumantasyon.txt`
- **النظام الثاني**: https://github.com/Laoode/EduView

---

## ✨ الملخص

**تشغيل بسيط:**
```
LAUNCH_SYSTEMS.bat → اختر النظام → استخدم!
```

**المنافذ:**
- Launcher: http://localhost:8080
- Online: http://localhost:5000  
- Classroom: http://localhost:3000

**🎉 استمتع بالنظام الموحد!**

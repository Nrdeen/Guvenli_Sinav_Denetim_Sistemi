# 🎓 دليل تشغيل نظام Güvenli Sınav - الاختبارات عن بُعد

## 📋 المتطلبات

### 1. تثبيت MySQL
- حمّل MySQL من: https://dev.mysql.com/downloads/mysql/
- اختر كلمة مرور قوية للـ root

### 2. تثبيت Python 3.8+
- يجب أن يكون Python مثبت مسبقاً

---

## 🚀 خطوات التثبيت

### **الخطوة 1: إعداد قاعدة البيانات**

افتح MySQL Command Line أو MySQL Workbench:

```sql
-- قم بتشغيل الملف
source "C:\Users\ASUS\Downloads\EduView-main\Güvenli Sınav Denetim Sistemi\database\schema.sql"
```

أو افتح الملف `database/schema.sql` ونفذه في MySQL.

### **الخطوة 2: تثبيت مكتبات Backend**

افتح PowerShell في مجلد Backend:

```powershell
cd "C:\Users\ASUS\Downloads\EduView-main\Güvenli Sınav Denetim Sistemi\backend"
pip install -r requirements.txt
```

### **الخطوة 3: إعداد ملف البيئة**

انسخ ملف `.env.example` إلى `.env`:

```powershell
Copy-Item .env.example .env
```

افتح `.env` وعدّل الإعدادات:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=كلمة_المرور_الخاصة_بك
DB_NAME=guvenli_sinav_db
```

---

## 🎬 تشغيل النظام

### **1. تشغيل الخادم (Backend API)**

في PowerShell:

```powershell
cd "C:\Users\ASUS\Downloads\EduView-main\Güvenli Sınav Denetim Sistemi\backend"
python main.py
```

سيعمل الخادم على: `http://localhost:8001`

### **2. تشغيل لوحة التحكم (للأستاذة)**

في PowerShell جديد:

```powershell
cd "C:\Users\ASUS\Downloads\EduView-main"
reflex run
```

افتح المتصفح: `http://localhost:3000/dashboard`

### **3. تشغيل برنامج الطالب (على جهاز الطالب)**

في PowerShell على جهاز كل طالب:

```powershell
cd "C:\Users\ASUS\Downloads\EduView-main\Güvenli Sınav Denetim Sistemi"
python student_client.py
```

سيطلب:
- رقم الطالب (مثال: STU001)
- رمز الاختبار (مثال: PROG2025)

---

## 📊 البيانات التجريبية

### الأستاذة:
- Username: `admin`
- Password: `admin123`

### رمز الاختبار:
- `PROG2025`

### الطلاب:
- STU001 - أحمد محمد علي
- STU002 - فاطمة حسن
- STU003 - محمد خالد
- STU004 - سارة يوسف
- STU005 - عمر إبراهيم

---

## 🔄 كيفية العمل

### **للأستاذة:**

1. شغلي الخادم (Backend)
2. شغلي لوحة التحكم على `http://localhost:3000/dashboard`
3. ستشاهدين جميع الطلاب المسجلين في الاختبار
4. عندما يبدأ الطلاب، ستظهر حالتهم "نشط" 🟢
5. ستظهر الانتهاكات فوراً في اللوحة

### **للطالب:**

1. شغّل `student_client.py`
2. أدخل رقمك (مثال: STU001)
3. أدخل رمز الاختبار (مثال: PROG2025)
4. ستفتح الكاميرا
5. البرنامج يرسل بياناتك للخادم كل 5 ثواني
6. اضغط 'q' للخروج

---

## 🌐 نشر النظام على الإنترنت (للاختبارات الفعلية)

### **الخيار 1: استضافة مجانية**

استخدم منصات مثل:
- **Railway.app** (مجاني)
- **Render.com** (مجاني)
- **PythonAnywhere** (مجاني)

### **الخيار 2: خادم جامعي**

اطلب من قسم IT تخصيص خادم وتثبيت النظام عليه.

---

## 📱 API Endpoints

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/exams/{exam_code}/info` | معلومات الاختبار |
| POST | `/api/violations` | تسجيل انتهاك |
| POST | `/api/heartbeat` | نبضة قلب الطالب |
| GET | `/api/exams/{exam_code}/students` | قائمة الطلاب وحالتهم |
| WS | `/ws/teacher/{exam_code}` | WebSocket للتحديثات الفورية |

---

## 🆘 حل المشاكل

### المشكلة: فشل الاتصال بقاعدة البيانات
**الحل:**
- تأكد من تشغيل MySQL
- تحقق من كلمة المرور في `.env`

### المشكلة: الطالب لا يظهر في اللوحة
**الحل:**
- تأكد من تشغيل الخادم (Backend)
- تحقق من صحة رمز الاختبار ورقم الطالب
- تأكد من أن الطالب مسجل في قاعدة البيانات

### المشكلة: الكاميرا لا تعمل
**الحل:**
- أعط الإذن للبرنامج باستخدام الكاميرا
- أغلق أي برنامج آخر يستخدم الكاميرا

---

## 📞 الدعم

لأي استفسارات، راجع الكود المصدري أو اتصل بمطور النظام.

# 🔧 دليل حل المشاكل الشائعة
# Troubleshooting Guide

## ❌ المشكلة: UNIQUE constraint failed: students.email

### الوصف:
```
sqlalchemy.exc.IntegrityError: UNIQUE constraint failed: students.email
```

### السبب:
محاولة إضافة طالب ببريد إلكتروني موجود بالفعل في قاعدة البيانات.

### الحل:

#### الحل 1: استخدام API المحدّث (تلقائي)
النظام الآن يتعامل مع هذه الحالة تلقائياً:
- إذا كان الطالب موجوداً، يُرجع بياناته بدلاً من إظهار خطأ
- إذا كان البريد مستخدماً من طالب آخر، يُظهر رسالة واضحة

#### الحل 2: تحديث بيانات الطالب
استخدم endpoint التحديث:
```http
PUT /api/students/{student_id}
Content-Type: application/json

{
  "student_id": "25619824009",
  "student_name": "Nrdeen Anber",
  "student_email": "nrdanber_new@gmail.com"
}
```

#### الحل 3: حذف الطالب القديم
```http
DELETE /api/students/25619824009
```

ثم إضافته من جديد:
```http
POST /api/students
Content-Type: application/json

{
  "student_id": "25619824009",
  "student_name": "Nrdeen Anber",
  "student_email": "nrdanber@gmail.com"
}
```

#### الحل 4: التحقق من البيانات الموجودة
استخدم أداة عرض قاعدة البيانات:
```powershell
python simple_db_viewer.py
# اختر: 3 (الطلاب)
```

---

## ❌ المشكلة: قاعدة البيانات غير موجودة

### الحل:
```powershell
cd "Güvenli Sınav Denetim Sistemi"
python backend/main.py
# انتظر حتى يبدأ الخادم، ثم اضغط Ctrl+C
```

---

## ❌ المشكلة: الطالب موجود لكن لا يمكن تسجيل الدخول

### الحل:
1. تحقق من رقم الطالب:
```powershell
python simple_db_viewer.py
# اختر: 3 (الطلاب)
```

2. تحقق من رمز الاختبار:
```powershell
python simple_db_viewer.py
# اختر: 2 (الاختبارات)
```

3. تحقق من التسجيل:
```powershell
python simple_db_viewer.py
# اختر: 4 (التسجيلات)
```

---

## ❌ المشكلة: البيانات التجريبية تتعارض مع البيانات الحقيقية

### الحل:
حذف قاعدة البيانات وإعادة إنشائها:

```powershell
# 1. أوقف الخادم (Ctrl+C)

# 2. احذف قاعدة البيانات القديمة
Remove-Item "backend/exam_monitoring.db"

# 3. شغّل الخادم من جديد
python backend/main.py
```

---

## 🔍 التحقق من البيانات

### عرض جميع الطلاب:
```powershell
python simple_db_viewer.py
# اختر: 3
```

### عرض طالب معين:
```sql
sqlite3 backend/exam_monitoring.db
SELECT * FROM students WHERE student_id = '25619824009';
.quit
```

### حذف طالب معين:
```sql
sqlite3 backend/exam_monitoring.db
DELETE FROM students WHERE student_id = '25619824009';
.quit
```

---

## 📝 نصائح لتجنب المشاكل

1. **استخدم أرقام طلاب فريدة**
   - تأكد أن كل طالب له رقم مختلف

2. **استخدم بريد إلكتروني فريد**
   - كل طالب يجب أن يكون له بريد مختلف

3. **احتفظ بنسخة احتياطية**
   ```powershell
   Copy-Item "backend/exam_monitoring.db" "backend/exam_monitoring_backup.db"
   ```

4. **راجع البيانات دورياً**
   ```powershell
   python simple_db_viewer.py
   ```

---

## 🆘 الحصول على المساعدة

إذا استمرت المشكلة:

1. **تحقق من السجلات (Logs)**
   - راجع رسائل الخطأ في Terminal

2. **استخدم أداة عرض قاعدة البيانات**
   ```powershell
   python simple_db_viewer.py
   ```

3. **أعد تشغيل النظام**
   ```powershell
   # أوقف كل شيء
   # ثم شغّل من جديد
   .\START_ALL.ps1
   ```

---

## ✅ التحقق من نجاح الحل

بعد تطبيق الحل، تحقق من:

1. **الخادم يعمل بدون أخطاء**
   ```powershell
   python backend/main.py
   # يجب أن ترى: "Application startup complete"
   ```

2. **يمكن إضافة طالب جديد**
   - جرب إضافة طالب من واجهة الويب

3. **يمكن تسجيل دخول الطالب**
   - جرب تسجيل الدخول برقم الطالب

---

## 📊 الأوامر المفيدة

### عرض جميع الطلاب:
```powershell
python simple_db_viewer.py
```

### حذف جميع الطلاب:
```sql
sqlite3 backend/exam_monitoring.db
DELETE FROM students;
.quit
```

### إعادة تعيين قاعدة البيانات:
```powershell
Remove-Item "backend/exam_monitoring.db"
python backend/main.py
```

---

**تم حل المشكلة! ✅**